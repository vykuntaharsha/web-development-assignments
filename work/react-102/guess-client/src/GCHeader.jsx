import React from 'react';

const GCHeader = ({title}) => {

    // header for the page
    return (
        <header className="gc-header">
            <h1> {title} </h1>
        </header>
    );
};

export default GCHeader;
